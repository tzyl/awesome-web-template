# awesome-web-template

Simple template for a project with React, Redux and Typescript with Less stylesheets and Webpack for building and hot reloading. Implements a basic counter app for demonstration.

## Usage

To run the hot-reloading development server on `localhost:3000`:

```
yarn
yarn start
```
